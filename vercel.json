{
  "version": 2,
  "buildCommand": "pnpm install && cd apps/frontend && pnpm run build:vercel",
  "outputDirectory": "apps/frontend/dist",
  "installCommand": "npm install -g pnpm@8.12.1 && pnpm install",
  "framework": null,
  "devCommand": null,
  "routes": [{ "handle": "filesystem" }, { "src": "/.*", "dest": "/index.html" }]
}
